# Never let direnv recreate a private framework deployment pin.
watch_file ../daemon-framework/flake.nix
use flake . --no-write-lock-file \
  --override-input daemonFramework "git+file://$(realpath ../daemon-framework)"
